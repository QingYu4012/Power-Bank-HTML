# Power Bank 串口调试助手

基于 Web Serial API 的网页版充电宝数据监测与串口透传调试工具，开箱即用、单页部署。

## 2026-04-17 功能实现记录 
1. 新增完整单页应用 [index.html](./index.html)。
2. 实现串口授权、端口列表刷新、连接/断开和热插拔监听。
3. 支持串口参数配置：波特率（自定义）、数据位、停止位、校验位、流控。
4. 支持字符串/HEX 双发送模式，支持自动换行（无/LF/CRLF/CR）。
5. 实现实时接收显示，支持字符串/HEX/字符串+HEX 三种接收视图。
6. 支持日志功能：实时日志、自动滚动、清空日志、保存日志到本地文件。
7. 支持定时循环发送：周期可配置，支持启动/停止，带防重入机制。
8. 支持文件发送：按分片发送、可设置分片大小与间隔、可取消并展示进度。
9. 完成响应式界面优化，适配桌面端和移动端布局。
10. 增加异常处理和状态反馈，优化连接失败、写入异常、设备断开等场景体验。

## 2026-04-18 V0.1

1. 将页面入口从 `Power Bank.html` 调整为 `index.html`，适配 GitHub Pages 默认访问路径。
2. 保留并整理 2026-04-17 完成的全部串口调试能力与界面实现。
3. 更新 README，补充 GitHub Pages 发布与访问说明。

## 功能清单

- 暗黑主题主监测界面（SOC/SOH、电池信息、端口信息、异常占位）
- USB 权限检测、连接/断开、热插拔监听与自动重连尝试
- 串口配置弹窗（端口选择、常用波特率、自定义波特率）
- RTC 读取与同步
- 协议通信（`AA BB` 帧头、`CRC8-CCITT` 校验、帧缓冲解析）
- 自动轮询（电池 1s、端口 400ms）
- 开发者模式（5 连击进入）
- 透传发送（文本/HEX、行尾附加、定时发送）
- 透传日志和收发字节统计
- USB 命令面板（GET_ACK/GET_INFO/GET_DATA/GET_RTC/SET_RTC/GET_PORT）
- 管理员面板（GET_TEST/SET_TEST）
- 响应式布局与安全上下文提示（HTTPS/localhost）

## 使用方法

1. 使用 Chrome 或 Edge（Chromium 内核）打开页面。
2. 点击“连接”，按浏览器弹窗完成 USB 授权；或先点状态按钮进入“虚拟串口设置”选择端口和波特率。
3. 连接成功后在主界面查看实时数据：SOC/SOH、电池信息、端口信息。
4. 如需校时，点击“同步RTC”。
5. 连续点击顶部标题 5 次进入“开发者模式”。
6. 在开发者模式可进行文本/HEX 透传发送、定时发送、查看日志与字节统计。
7. 点击“命令”进入 USB 命令面板，按需发送 GET_DATA / GET_RTC / GET_PORT 等命令。
8. 如需测试命令，先“管理员登录”后使用 GET_TEST / SET_TEST。
9. 调试结束点击“断开”。

## GitHub Pages 发布

你的仓库地址是：`https://github.com/QingYu4012/Power-Bank-HTML`

开启 GitHub Pages 后，访问地址是：
`https://qingyu4012.github.io/Power-Bank-HTML/`

设置步骤：

1. 打开仓库 `Settings`。
2. 进入 `Pages`。注意要使用Pages功能仓库必须是公开的
3. `Build and deployment` 选择 `Deploy from a branch`。
4. Branch 选择 `master`，目录选择 `/ (root)`，点击保存。
5. 等待 1-3 分钟后刷新上面的访问地址。

## 部署说明

- 本项目为纯静态页面，无需后端服务。
- 入口文件为 [index.html](./index.html)。
- Web Serial API 仅在安全上下文可用，请使用以下方式访问：
  - `https://` 域名
  - `http://localhost`

## 目录结构

- [index.html](./index.html)：主页面与全部前端逻辑
- [README.md](./README.md)：项目说明与更新记录

## 2026-04-18 V0.2
- 发布 `GitHub Pages`


## 2026-04-18 V0.3
- 完善修改 `README` 文件

## 2026-04-19 V0.4
1. 按桌面 `Power Bank` Android 项目完成 Web 端 1:1 功能复刻，界面切换为暗黑色系主题。
2. 新增主监测视图：顶部连接状态、手机时间/充电宝时间、SOC/SOH 环形显示、电池信息卡、端口信息卡、异常信息卡。
3. 新增 RTC 同步能力：`同步RTC` 按钮发送 `SET_RTC`，随后自动 `GET_RTC` 刷新显示。
4. 完成协议层实现：`AA BB + CMD + LEN + DATA + CRC8-CCITT` 的封包、解包、CRC 校验与粘包缓冲解析。
5. 实现常规模式自动轮询：
   - 电池轮询约 1s：`GET_ACK`、`GET_RTC`、`GET_DATA`（动态类型 + 静态补拉）
   - 端口轮询约 400ms：`GET_PORT` 轮询端口 `0..3`
6. 新增开发者模式（标题 5 连击进入）与透传面板：
   - 文本/HEX 发送
   - 行尾附加（无、`\n`、`\r\n`）
   - 定时发送
   - 收发日志和字节统计
7. 新增 USB 命令面板，支持：
   - `GET_ACK`、`GET_INFO`
   - `GET_DATA`
   - `GET_RTC / SET_RTC`
   - `GET_PORT`
   - 管理员权限下 `GET_TEST / SET_TEST`
8. 新增管理员登录弹窗和测试参数面板，支持参数范围提示、快捷值、设备值回读。
9. 本轮细节修正：
   - 管理员登录控件文案补齐（管理员名称、密码标签、显示密码/隐藏密码）
   - 提示气泡改为顶部短提示（更接近 Android 交互）
   - 热插拔检测到新 USB 端口时自动尝试连接
   - 修复发送区占位文案与日志转义细节

## 2026-04-19 V0.5
1. 虚拟串口设置中的“端口选择”交互改造：
   - 删除下拉选择控件，改为上方文本直接展示当前授权端口信息。
2. 端口展示文案统一为：
   - 已授权端口：VID：xxxx    PID：xxxx
   - 未授权时默认显示 VID：----    PID：----。
3. 在“虚拟串口设置”弹窗新增 获取授权 按钮，可直接触发串口授权流程（无需退出弹窗）。
4. 串口授权策略调整为“单端口保留”：
   - 重新授权时会清理旧授权，仅保留当前授权端口（浏览器支持 orget() 时自动执行）。
   - 对不支持 orget() 的环境给出手动清理提示。
5. 同步 RTC 逻辑改为“设备回包确认成功”：
   - 点击 同步RTC 后不再立即提示成功。
   - 只有收到设备 CMD_RTC 回复帧时才提示 RTC同步成功。
   - 增加超时保护（5 秒未收到回包提示失败）与等待状态防重入。
6. 断连/清理流程中加入 RTC 等待状态清除，避免断连后出现误提示。
7. 顶部短提示（toast）停留时长优化：
   - 默认时长由 800ms 调整为 2200ms，并设置最短停留 1800ms，避免“闪一下看不到”。
8. index.html 全文件进行了统一格式化整理（仅代码样式调整，不改变业务逻辑）。

## 2026-04-19 V0.6
1. 端口信息展示改为“田字形”布局（2x2 网格）：
   - 将端口卡片容器从纵向列表改为双列网格，四个端口按两行两列显示。
   - 移除端口卡片之间的分隔线插入逻辑，避免破坏四宫格结构。
2. 电池信息展示改为“田字形”布局（四宫格）：
   - 将原“2块 + 3块分散”结构重排为统一的 2x2 网格结构。
   - 第三个象限合并“预计充满/预计放空 + 容量信息”，第四象限保留电池类型、厂商、生产日期、批次序列号。
3. 主数据对齐优化（名称 / 数据 / 单位）：
   - 调整 `kv` 列布局为 `名称 + 中间数据 + 单位`，数据列居中显示并与单位列分离。
   - 电池类型等无单位项同步按“名称 + 中间数据”规则对齐，保持视觉一致。
4. 端口小字信息可读性优化：
   - 端口角色与协商信息改为结构化小字布局，拆分为“名称 / 数据 / 单位”三段显示。
   - 增加小字行距、列间距与字距，避免内容过于紧凑。
5. 数值显示稳定性优化：
   - 数据值列启用 `tabular-nums`，提升 `---` 与实时数字切换时的对齐稳定性。
6. 手机端响应式断点与视口适配增强：
   - 在原有断点基础上新增 `820px` 与 `420px` 细分规则，覆盖平板窄屏与小屏手机场景。
   - `shell` 视口高度补充 `100dvh`，改善移动端地址栏伸缩导致的高度抖动。
7. 顶部状态区与连接操作区移动端排版优化：
   - 顶部标题区域支持换行与自适应字号，避免小屏下标题与状态标识互相挤压。
   - 连接按钮区在中小屏启用弹性布局，在手机端改为单列满宽，提升可点击性。
8. 端口信息卡片在手机端改为单列显示：
   - 在 `<=820px` 时将端口区从双列切换为单列，减少信息拥挤，提升阅读效率。
9. 移动端卡片与弹窗可读性优化：
   - 小屏下统一调整卡片内边距、圆角、字段字号与列间距，降低视觉压迫感。
   - 弹窗在手机端改为更贴边宽度与更高可用高度，底部按钮支持换行排布。
10. 修复内联样式覆盖媒体查询的问题：
    - 将“发送附加换行”区域的内联 `grid-template-columns` 改为类样式控制，确保手机断点规则可生效。

## 2026-04-19 V0.7
1. 开发者模式交互重构：
    - 移除开发者模式中的“命令”按钮，将 USB 命令面板改为开发者模式右侧常驻显示。
    - 进入开发者模式时自动初始化 USB 命令区，保留管理员登录、GET/SET_TEST 与原 USB 命令功能。
2. 管理员测试区可读性与间距优化：
    - 扩大测试区内边距、模块间距、文字行高、按钮高度与输入框高度，减少信息拥挤。
    - 将参数范围/设备当前值/测试状态整合为独立信息分组，提升层次感。
3. 测试快捷值布局增强（含 CycleCount）：
    - CycleCount 的快捷值 `70 / 140 / 210` 调整为同一行显示。
    - 快捷值网格根据参数类型动态切换 2 列/3 列，小屏自动退化为单列，兼顾桌面与移动端。
4. “发送附加换行”交互改为下拉列表：
    - 将“无 / `\\n` / `\\r\\n`”从单选改为下拉选择。
    - 同步更新 `curLine()` 与事件绑定逻辑，保持发送行为与原协议处理一致。

## 2026-04-19 V0.8
1. 开发者模式新增“日志保存”按钮，位置在“清空”左侧；点击后可将透传日志导出为本地 txt 文件。
2. 日志文件名改为保存时刻命名，格式为 `YYYY-MM-DD HH:mm:ss.txt`。
3. 日志导出内容改为对齐结构：每条日志按三行输出（时间与方向、HEX、TEXT），多条日志间保留空行。
4. 透传设置区布局优化：`HEX发送`、`HEX显示`、`定时发送` 同行显示，并将定时发送间隔输入框与定时发送同排。
5. “发送附加换行”区域改为左右两列：左侧“超时时间(ms)”输入，右侧“发送附加换行”下拉，宽度各占一半。
6. 新增接收分帧超时参数：默认 `20ms`，范围 `1-5000ms`；连续数据间隔超过阈值时，后续数据判定为下一帧。
7. 分帧缓冲在读取结束、断开连接、手动清空时会主动冲刷，避免尾帧残留。
8. 增大发送区输入框高度，提升长报文编辑体验。

## 2026-04-20 V0.9
1. 按 `SC2016 DEMO USB 通讯协议` 对齐数据流命令解析，`USB_CMD_GET_PROT_DATA (0x05)` 保持严格 15 字节解析，不兼容 14 字节响应。
2. 补齐管理员测试参数：新增 `USB_CMD_GET_TEST / SET_TEST` 的 `0x02`（设备已使用月份）读写与显示。
3. 断开连接后统一恢复默认数据显示：所有 `disconn` 断开路径默认执行 `resetData()`，避免旧数据残留。
4. 端口数据显示增加强门控：仅当“端口已使能 且 电源角色不为 `NONE` 且 已收到有效端口数据”时显示实时值，否则保持 `---`。
5. 协议处理改为“接收驱动更新”：发送后不再依赖请求-回复配对，不等待指定回复；仅当收到并通过协议合法性校验（帧头/长度/CRC/字段范围）的数据包时更新对应控件，否则忽略。
6. 轮询策略明确化并落地：
   - 动态数据每 `1000ms` 发送一次获取命令；
   - 静态常量数据在首次收到有效数据前每 `1000ms` 轮询，收到一次后停止该项轮询；
   - 端口数据固定每 `400ms` 轮询一次。
7. 修复端口数据串口位问题：端口回包归属仅使用回包反馈端口号，并增加普通模式下的端口帧顺序校验，抑制端口数据显示“跳端口/闪回默认值”。
8. 修正预计时间字段显示：`预计充满/预计放空` 按 2 字节分钟值解析；过滤 `0xFFFF` 等无效值；并按充放电状态门控显示，不满足状态时显示 `---`。
9. 调整端口卡片“握手协议”一行布局：改为两列结构并移除空占位，使协议值更靠左且在可用区域内保持中间对齐。

## 2026-04-27 V1.0
1. 更新 `USB_CMD_GET_PROT_DATA / GET_PORT` 的 15 字节响应解析：
   - 取消原 `Byte0` 端口使能字节，不再通过端口使能掩码判断端口是否启用。
   - 原端口数据整体前移 1 字节，`Byte0..Byte13` 直接解析端口状态、PD/DPDM 协议、电压电流、保护标志等数据。
   - 新增 `Byte14` 端口类型字段：`PORT_TYPE_NONE` 表示端口未使能，其余类型表示端口已使能。
2. 端口卡片显示门控同步改为依据 `Byte14` 端口类型判断，端口类型为 `PORT_TYPE_NONE` 时忽略前面所有端口数据并保持实时值 `---`。
